---
perform_sweep: True

paths:
  model_note: default_percolation

parameter_space:
  num_steps: 100
  NotchDelta:
    rate_ph:
      rate_0: !sweep
        default: 8e-3
        linspace: [0.01, 0.06, 4]
        name: beta
