---
perform_sweep: True

paths:
  model_note: default_percolation

parameter_space:
  num_steps: 100
  NotchDelta:
    rate_ph:
      beta: !sweep
        default: 8e-3
        linspace: [4e-3, 8e-3, 4]
      rate_0: !coupled-sweep
        target_name: beta